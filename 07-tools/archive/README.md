# 07-tools Archive

Purpose
- Holds legacy one-off scripts removed from active tool paths during cleanup.

Subfolders
- legacy-debug/: temporary debug helpers and wrappers
- legacy-manual-tests/: manual script tests superseded by formal tests
- legacy-query-scripts/: ad hoc query/check scripts no longer in active workflow

Guideline
- Prefer using active scripts in analysis/, extraction/, generation/, validation/.
